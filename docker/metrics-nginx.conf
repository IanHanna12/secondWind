server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    location = /metrics.prom {
        default_type "text/plain; version=0.0.4; charset=utf-8";
        add_header Cache-Control "no-store";
        try_files /metrics.prom =503;
    }
}
